import React from 'react'

const CourseDetailLayout = ({children}) => {
  return (
    <div>
        This is the layout of course detail
        {children}
    </div>
  )
}

export default CourseDetailLayout